name: Pull Request

on:
  pull_request:
    branches: [main]

jobs:
  requests:
    runs-on: ubuntu-latest
    steps:
    - name: Add Reviewers
      uses: madrapps/add-reviewers@v1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        reviewers: lowkh


